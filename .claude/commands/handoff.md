---
description: يعيد توليد HANDOFF.md في جذر المشروع بنفس البنية القياسية
---

# مهمة: أعِد توليد `HANDOFF.md`

أعد كتابة الملف `HANDOFF.md` في جذر المشروع (`/Users/machhm/Desktop/cloud_skils/khazaabalat/HANDOFF.md`) بالكامل، بحيث يعكس **الحالة الحالية للعمل** بدقّة.

## قواعد التنفيذ
- **لا تُعدّل أي كود مشروع** — هذا تحديث توثيق فقط.
- **استخدم subagent** (Explore/general-purpose) لأي استكشاف واسع لمعرفة ما تغيّر، وارجع بالخلاصة فقط. لا تقرأ ملفات كبيرة في السياق الرئيسي؛ استخدم `Grep` المحدّد.
- **استخرج النسخ والأوامر من `package.json`/الإعدادات حرفيًا — لا تخترع** أوامر أو إصدارات.
- إن لم يتغيّر شيء منذ آخر نسخة، حدّث فقط سطر «آخر تحديث» وأبقِ المضمون.
- حدّث سطر «آخر تحديث» إلى تاريخ اليوم (من بيئة الجلسة `currentDate`).
- بعد الكتابة، شغّل `npm run build` (بعد تفعيل nvm) للتحقّق من عدد الصفحات/الأخطاء وضع الرقم الفعلي في الوثيقة. وجّه المخرجات الضخمة لملف واقرأ منها `| tail` فقط.

## البنية القياسية (التزم بها حرفيًا — نفس العناوين والترتيب)
```
# HANDOFF — مشروع «خزعبلات»

_آخر تحديث: <YYYY-MM-DD>_

> سطر/سطران عن الحالة العامة.

---

## 1) الهدف الحالي
<الهدف النهائي + المرجع + نقطة القرار المفتوحة إن وُجدت>

## 2) ما تم إنجازه (ملفات ودوال محدّدة)
<مقسّم: البيانات/الأنماط/القوالب/المكوّنات/الصفحات/الدوال التفاعلية — بأسماء الملفات والدوال، وأي تحذيرات كود ميت أو روابط placeholder>

## 3) الخطوات المتبقية (مرقّمة بالترتيب)
<قائمة مرقّمة 1..n بالترتيب المنطقي للتنفيذ>

## 4) القرارات المهمة وأسبابها
<نقاط: القرار → السبب>

## 5) المحاولات الفاشلة (حتى لا تتكرّر)
<نقاط: ما فشل → ليش → البديل الصحيح>

## 6) أوامر التشغيل والاختبار
<كتلة bash بأوامر package.json الفعلية + تفعيل nvm + ملاحظة غياب lint/test إن صحّ>

## ملاحظات بيئة
<macOS/Node/nvm/symlink، وحسابات المراحل القادمة>
```

## بعد الانتهاء
اعرض للمستخدم سطرًا واحدًا يؤكّد التحديث + تاريخ «آخر تحديث» الجديد + نتيجة البناء (عدد الصفحات/الأخطاء).
